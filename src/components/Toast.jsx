import React from "react";
import { Toaster } from "react-hot-toast";

const Toast = () => {
  return <Toaster position="top-right" />; // Position the toasts in the top-right corner
};

export default Toast;
